#!/bin/sh
# Run offline selfcheck before commit. Install: scripts/install-precommit.ps1
set -e
cd "$(git rev-parse --show-toplevel)"
if command -v npm >/dev/null 2>&1; then
  npm run precommit
else
  node --experimental-strip-types scripts/selfcheck.mjs
fi
